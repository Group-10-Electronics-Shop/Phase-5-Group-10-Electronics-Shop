import React from "react";

export default function Login(){
  return (
    <div className="container-inline" style={{padding:20, background:"#fff", borderRadius:12}}>
      <h2>Login</h2>
      <p>Implement login form here (connect to backend /api/auth/login).</p>
    </div>
  );
}